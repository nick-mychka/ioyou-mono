# This file will be committed to version control, so make sure not to have any secrets in it.

# Database connection URL
POSTGRES_URL="postgresql://postgres:postgres@localhost:5432/ioyou"

# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='supersecret'

# Google OAuth — optional, omit to disable Google sign-in
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''

# Apple OAuth — optional, omit to disable Apple sign-in
# Required on iOS App Store if any other social login is offered
# @see https://www.better-auth.com/docs/authentication/apple
AUTH_APPLE_ID=''
AUTH_APPLE_SECRET=''
